import styled from 'styled-components'

const Cabecalho = styled.header` background-color: var(--cor-principal)
color: var(--cor-principal)
text-align: center
padding: 24px 0 
`

export default Cabecalho
